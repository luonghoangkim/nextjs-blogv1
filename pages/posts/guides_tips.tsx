import Layout from "../../components/layuot"

const GuidesTips = () => {
    return(
        <Layout>
            <div>Thủ thuật và Hướng dẫn ở đây</div>
        </Layout> 
    );
}

export default GuidesTips;